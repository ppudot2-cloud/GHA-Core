# =============================================================================
# COMPOSITE ACTION: CI Bootstrap
# =============================================================================
# Shared first-step group used by both _job-build and _job-deploy.
# Performs: repo checkout, GHA-Core CI checkout, Azure OIDC login,
#           AKV secret fetch, and global/project variable merge.
#
# NOTE: Composite actions cannot access ${{ secrets.* }} directly.
#       Callers must expose GHA_CORE_PAT via env: GHA_CORE_PAT on the step.
# =============================================================================
name: 'CI Bootstrap'
description: 'Checkout repos, Azure OIDC login, fetch AKV secrets, merge global+project variables'

inputs:
  mock_deploy:
    description: 'Skip Azure login and AKV fetch in dry-run mode'
    required: false
    default: 'false'
  jfrog_enabled:
    description: 'Also fetch jfrog-api-key from AKV (set true when jfrog_url is provided)'
    required: false
    default: 'false'

runs:
  using: composite
  steps:
    - name: Checkout calling repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Checkout GHA-Core CI scripts
      uses: actions/checkout@v4
      with:
        repository: ppudot2-cloud/GHA-Core
        ref: main
        path: .ci
        token: ${{ env.GHA_CORE_PAT }}

    - name: Azure Login (OIDC)
      if: ${{ inputs.mock_deploy != 'true' }}
      uses: azure/login@v2
      with:
        client-id:       ${{ vars.AZURE_CLIENT_ID }}
        tenant-id:       ${{ vars.AZURE_TENANT_ID }}
        subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

    - name: Fetch secrets from Azure Key Vault
      if: ${{ inputs.mock_deploy != 'true' }}
      shell: pwsh
      run: |
        $kvName       = '${{ vars.AZURE_KEY_VAULT_NAME }}'
        $jfrogEnabled = '${{ inputs.jfrog_enabled }}' -eq 'true'
        $secrets = [ordered]@{
          'pp-app-id'        = 'PP_APP_ID'
          'pp-client-secret' = 'PP_CLIENT_SECRET'
          'pp-tenant-id'     = 'PP_TENANT_ID'
        }
        if ($jfrogEnabled) { $secrets['jfrog-api-key'] = 'JFROG_API_KEY' }
        foreach ($entry in $secrets.GetEnumerator()) {
          $value = az keyvault secret show `
            --vault-name $kvName --name $entry.Key --query value -o tsv 2>&1
          if ($LASTEXITCODE -ne 0) {
            Write-Error "Failed to fetch AKV secret '$($entry.Key)': $value"; exit 1
          }
          Write-Host "::add-mask::$value"
          "$($entry.Value)=$value" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          Write-Host "  ✔ Fetched AKV:'$($entry.Key)' → ENV:'$($entry.Value)'"
        }
        Write-Host "✅ All secrets fetched from Azure Key Vault: $kvName"

    - name: Merge global and project variables
      shell: pwsh
      run: |
        pwsh .ci/templates/steps/dynamics/Merge-Variables.ps1 `
          -GlobalVarsPath  ".ci/templates/variables/dynamics/global-vars.yml" `
          -ProjectVarsPath ".github/config/project-vars.yml"
